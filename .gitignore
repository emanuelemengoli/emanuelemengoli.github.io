_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
assets/libs/
node_modules/
vendor

# bin/sync-wines local state (see bin/wines-sync.md)
bin/wines_csv_url.txt
bin/.wines_geocache.json

# local dev launcher — machine-specific, never committed
bin/local_loader

# source archives dropped into the image tree — extract, don't commit
assets/img/*.zip